============================================================================
 PROMPTS USED FOR GENERATIVE AI ASSIGNMENT #1
 Question 1: Machine Translation (English → Urdu)
 Student: Muhammad Idrees (i230582)
============================================================================

PROMPT 1 — Initial Assignment Implementation:
"Implement the complete Generative AI Assignment #1: English-to-Urdu
Machine Translation using vanilla RNN encoder-decoder. The assignment
requires:
(1) Data Preprocessing - load the Kaggle english_to_urdu_dataset.xlsx,
    clean and normalize English and Urdu text separately
(2) Train-Validation-Test Split - 80/10/10 with seed=42, verify no overlap
(3) Tokenization and Vocabulary Construction with special tokens
    PAD, SOS, EOS, UNK and minimum frequency threshold
(4) Sequence Encoding, Padding, and Batching using PyTorch DataLoader
    with sort-by-length and pack_padded_sequence
(5) Vanilla RNN Encoder-Decoder Model using nn.RNN only -
    NO LSTM, GRU, or Transformer
(6) Model Training with CrossEntropyLoss, Adam optimizer, gradient
    clipping, ReduceLROnPlateau scheduler, teacher forcing decay,
    early stopping, and checkpoint saving
(7) Hyperparameter Tuning using Grid Search over 8 configurations
    (embed dim, hidden dim, layers, learning rate, dropout, batch size)
    for 5 epochs each, then retrain best config for 30 epochs
(8) Inference with greedy decoding and beam search (k=5),
    BLEU-1/2/3/4 score evaluation on test set
(9) Error Analysis of 15+ sentences with sentence-level BLEU
    classification (GOOD/PARTIAL/POOR) and discussion of vanilla RNN
    limitations. Device: cuda (Google Colab)."

PROMPT 2 — Fix Dataset Loading Error:
"The code has an error where it cannot find the dataset file. The dataset
is english_to_urdu_dataset.xlsx from Kaggle. Fix the dataset loading to:
- Support Google Colab file upload via google.colab.files
- Use pd.read_excel() with openpyxl for .xlsx files
- Auto-detect English and Urdu columns from column names
- Search multiple common paths (/content/, working directory, etc.)
- Validate the file has 2 columns with text data"

PROMPT 3 — Complete Implementation with All Tasks:
"Write the complete Q1_Machine_Translation.py script that:
- Loads english_to_urdu_dataset.xlsx (9,103 sentence pairs)
- Implements all 9 assignment tasks with detailed output
- Uses vanilla RNN (nn.RNN) only, verified programmatically
- Runs on Google Colab with CUDA GPU support
- Saves training_curves.png and best_model_curves.png plots
- Saves best_nmt.pth and best_nmt_tuned.pth checkpoints
- Prints comprehensive results including BLEU scores and error analysis"

PROMPT 4 — LaTeX Report Generation:
"Write a comprehensive research-style technical report in Springer LNCS
format (llncs document class) for the English-to-Urdu Machine Translation
assignment. Include:
- Abstract summarizing objective, methodology, dataset (9,103 pairs),
  and actual results (BLEU-1=0.1158 greedy, BLEU-4=0.0062 beam search)
- Introduction defining the translation problem and English-Urdu challenges
- Methodology section with all 9 tasks: preprocessing, tokenization
  (EN vocab=3,969, UR vocab=4,313), model design (vanilla RNN encoder-
  decoder, 6,171,865 params), and training setup
- Experimental results with actual Grid Search table (8 configs, best=
  embed 256, hidden 512, 2 layers, lr=5e-4, dropout=0.3, batch=64),
  training convergence analysis (best val loss=5.6000), BLEU score table,
  and representative translation examples
- Discussion with error analysis (15 sentences, 100% POOR, dominant errors:
  word order/missing words=15, repetition=2) and vanilla RNN limitations
  (vanishing gradients, context bottleneck, no gating)
- Conclusion summarizing findings and future work (attention, LSTM/GRU,
  Transformer, BPE tokenization, data augmentation)
- References in Springer LNCS bibliography format"
